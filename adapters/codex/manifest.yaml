name: codex
version: 0.3
source: auth-signin
description: "Codex CLI — OpenAI auth sign-in from E:\\CLAUDE's-N-Joshua's-Dream-Online-MMORPG. Direct CLI lane; do not use Paperclip codex_local mini routing for real work."
owner: sabretooth
node_id: sabretooth
cli_command: "codex"
health_check: "codex --version"
paperclip_adapter_type: "codex_local"
paperclip_adapter_config:
  cwd: "E:\\CLAUDE's-N-Joshua's-Dream-Online-MMORPG"
  model: "codex"
paperclip_alias: "codex"
auth:
  type: browser-signin
  instructions: "Run `codex login` from E:\\CLAUDE's-N-Joshua's-Dream-Online-MMORPG, or `codex login --device-auth` if device/browser auth is needed. Check with `codex login status`."
notes: "Codex CLI auth lane. No adapter API key. Use direct Codex CLI with local config (gpt-5.5 on this machine). Do not route real Codex work through codex-mini. Pi can use openai-codex/gpt-5.5; OpenCode/Hermes fallback routes must use Codex-named models such as opencode/gpt-5.3-codex or openrouter/openai/gpt-5.3-codex, not generic openai/*."

