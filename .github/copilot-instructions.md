# Copilot instructions

Follow `AGENTS.md` at the repository root and `C:\DREAM\AGENTS.md` above it. Copilot is not a judge lane: commit on a branch, run `npm test` in `game/server/live-npc-lab`, and hand the work to Claude Code or Codex with a report.
